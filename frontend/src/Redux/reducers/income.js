const incomeReducer = (state, action)=>{
    switch(action.type) {
        case 'addIncome': {
            return state
        }
        case 'getIncome': {
            return state
        }
        default : {
            return state
        }
    }
}

export default incomeReducer;