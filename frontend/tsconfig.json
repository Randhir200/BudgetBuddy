{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "jsx": "react-jsx", // For React 17+ JSX transform, use "react-jsx" or "react-jsxdev"
    "lib": ["dom", "esnext"] // Ensure DOM types are included
    // Other necessary options...
  }
}
